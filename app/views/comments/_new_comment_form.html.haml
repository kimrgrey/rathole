= form_tag comments_path(post_id: post.id), remote: true, id: 'new-comment-form', class: 'form-horizontal' do
  .form-group
    .col-sm-12
      = text_area_tag :body, '', rows: 15, class: 'form-control', placeholder: t('.placeholders.body')
  .form-group
    .col-sm-12
      = submit_tag t('.save'), class: 'btn btn-primary'