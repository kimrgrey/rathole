%table.table.table-bordered.table-hover
  %thead
    %tr
      %th= Post.human_attribute_name(:id)
      %th= Post.human_attribute_name(:title)
      %th= Post.human_attribute_name(:user_name)
      %th= Post.human_attribute_name(:created_at)
  %tbody
    - posts.each do |post|
      %tr{class: post_css_class(post), data: {url: admin_post_url(post)}}
        %td= post.id
        %td= post.title
        %td= post.user_name
        %td= l(post.published_at.present? ? post.published_at : post.created_at)