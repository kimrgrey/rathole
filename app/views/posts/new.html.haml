= content_for :nav do
  = render partial: 'public/nav', locals: { user: current_user }
  = render partial: 'users/my', locals: { user: current_user }

.container
  = clip_and_clamp
  = page_header
  = render partial: 'posts/form', locals: { post: @post }