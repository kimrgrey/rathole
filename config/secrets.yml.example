development:
  secret_key_base: # generate secret key with rake secret 
  devise_key:      # generate secret key with rake secret
  devise_email:    # email address that will be used by devise

test:
  secret_key_base: # generate secret key with rake secret 
  devise_key:      # generate secret key with rake secret
  devise_email:    # email address that will be used by devise

production:
  secret_key_base: # generate secret key with rake secret 
  devise_key:      # generate secret key with rake secret
  devise_email:    # email address that will be used by devise
  mandrill_user:   # user name from https://mandrillapp.com
  mandrill_key:    # secret key from https://mandrillapp.com